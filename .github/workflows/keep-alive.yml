name: Keep Alive - Ping Backend

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: |
          curl -f https://hotelmanagement-69l6.onrender.com/health || echo "Ping failed"
